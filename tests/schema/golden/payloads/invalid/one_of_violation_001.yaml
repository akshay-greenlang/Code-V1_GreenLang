# Invalid Payload - OneOf Violation
# Error Code: GLSCHEMA-E405 (ONE_OF_VIOLATION)
# Schema: rules/dependency_rules.yaml

_test_metadata:
  schema: "rules/dependency_rules.yaml"
  description: "OneOf violation - type is government but missing organization_name (matches 0 schemas)"
  expected_valid: false
  expected_errors:
    - code: "GLSCHEMA-E405"
      path: ""
      severity: "error"

id: "ABCD1234"
type: "government"
# Missing organization_name which is required for government type
# This means type matches const but required field is missing → no match
