"""Basic constraint test schemas.

This package contains test schemas for basic JSON Schema constraints:
    - string_constraints.yaml: minLength, maxLength, pattern, format
    - numeric_constraints.yaml: minimum, maximum, exclusiveMinimum, exclusiveMaximum, multipleOf
    - array_constraints.yaml: minItems, maxItems, uniqueItems, contains
    - object_constraints.yaml: required, additionalProperties, minProperties, maxProperties
    - enum_constraints.yaml: enum, const
    - type_constraints.yaml: type validation for all JSON types
    - ref_schema.yaml: $ref resolution, $defs
    - combined_schema.yaml: Complex schema with multiple constraint types
"""
