---
# AGENT-DATA-012: GL-DATA-X-015 Missing Value Imputer - Service
apiVersion: v1
kind: Service
metadata:
  name: missing-value-imputer-service
  namespace: greenlang
  labels:
    app: missing-value-imputer-service
    agent-id: GL-DATA-X-015
    component: missing-value-imputation
spec:
  type: ClusterIP
  selector:
    matchLabels:
      app: missing-value-imputer-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
