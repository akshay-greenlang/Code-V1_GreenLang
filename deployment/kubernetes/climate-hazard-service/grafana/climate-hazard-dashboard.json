{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "title": "Assessments (rate/min)",
      "type": "timeseries",
      "gridPos": { "h": 6, "w": 6, "x": 0, "y": 0 },
      "targets": [{ "expr": "rate(gl_chc_assessments_total[5m]) * 60", "legendFormat": "{{status}}" }]
    },
    {
      "title": "Assessment Duration (p95)",
      "type": "timeseries",
      "gridPos": { "h": 6, "w": 6, "x": 6, "y": 0 },
      "targets": [{ "expr": "histogram_quantile(0.95, rate(gl_chc_assessment_duration_seconds_bucket[5m]))", "legendFormat": "p95" }]
    },
    {
      "title": "High Risk Locations",
      "type": "gauge",
      "gridPos": { "h": 6, "w": 6, "x": 12, "y": 0 },
      "targets": [{ "expr": "gl_chc_high_risk_locations", "legendFormat": "High Risk" }],
      "fieldConfig": {
        "defaults": {
          "min": 0,
          "max": 500,
          "thresholds": {
            "steps": [
              { "color": "green", "value": 0 },
              { "color": "yellow", "value": 50 },
              { "color": "red", "value": 100 }
            ]
          }
        }
      }
    },
    {
      "title": "Active Locations Monitored",
      "type": "stat",
      "gridPos": { "h": 6, "w": 6, "x": 18, "y": 0 },
      "targets": [
        { "expr": "gl_chc_active_locations", "legendFormat": "Active Locations" },
        { "expr": "gl_chc_active_hazard_types", "legendFormat": "Hazard Types" }
      ]
    },
    {
      "title": "Projections (rate/min)",
      "type": "timeseries",
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 6 },
      "targets": [{ "expr": "rate(gl_chc_projections_total[5m]) * 60", "legendFormat": "{{status}}" }]
    },
    {
      "title": "Data Source Errors (rate/hour)",
      "type": "timeseries",
      "gridPos": { "h": 6, "w": 8, "x": 8, "y": 6 },
      "targets": [{ "expr": "rate(gl_chc_data_source_errors_total[1h]) * 60", "legendFormat": "{{source}}" }]
    },
    {
      "title": "Reports Generated (rate/min)",
      "type": "timeseries",
      "gridPos": { "h": 6, "w": 8, "x": 16, "y": 6 },
      "targets": [{ "expr": "rate(gl_chc_reports_generated_total[5m]) * 60", "legendFormat": "{{report_type}}" }]
    },
    {
      "title": "Hazard Types Distribution",
      "type": "bargauge",
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 12 },
      "targets": [{ "expr": "sum by (hazard_type) (gl_chc_hazard_detections_total)", "legendFormat": "{{hazard_type}}" }]
    },
    {
      "title": "Processing Duration by Operation (p95)",
      "type": "timeseries",
      "gridPos": { "h": 6, "w": 8, "x": 8, "y": 12 },
      "targets": [{ "expr": "histogram_quantile(0.95, rate(gl_chc_processing_duration_seconds_bucket[5m]))", "legendFormat": "{{operation}}" }]
    },
    {
      "title": "Scenario Analysis Results",
      "type": "timeseries",
      "gridPos": { "h": 6, "w": 8, "x": 16, "y": 12 },
      "targets": [{ "expr": "rate(gl_chc_scenario_analyses_total[5m]) * 60", "legendFormat": "{{scenario}}" }]
    },
    {
      "title": "Pod Resource Usage",
      "type": "timeseries",
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 18 },
      "targets": [
        { "expr": "container_memory_working_set_bytes{pod=~\"climate-hazard.*\"} / 1024 / 1024", "legendFormat": "Memory (MB)" },
        { "expr": "rate(container_cpu_usage_seconds_total{pod=~\"climate-hazard.*\"}[5m]) * 1000", "legendFormat": "CPU (millicores)" }
      ]
    },
    {
      "title": "Requests Total (rate/min)",
      "type": "timeseries",
      "gridPos": { "h": 6, "w": 8, "x": 8, "y": 18 },
      "targets": [{ "expr": "rate(gl_chc_requests_total[5m]) * 60", "legendFormat": "{{status}}" }]
    }
  ],
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["greenlang", "climate-hazard", "agent-data-020", "data-geo"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "title": "Climate Hazard Connector - GL-DATA-GEO-002",
  "uid": "climate-hazard-overview",
  "version": 1
}
