# =============================================================================
# GreenLang ERP/Finance Connector - Network Policies
# =============================================================================
# Enforces network isolation for the ERP connector service within the
# greenlang namespace. Default deny all, then allow specific ingress/egress.
# PRD: AGENT-DATA-003
# =============================================================================

---
# =============================================================================
# Default Deny All - Baseline policy for ERP connector pods
# =============================================================================

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: erp-connector-default-deny-all
  namespace: greenlang
  labels:
    app.kubernetes.io/name: erp-connector-service
    app.kubernetes.io/component: network-policy
    app.kubernetes.io/part-of: greenlang
  annotations:
    description: "Default deny all ingress and egress traffic for ERP connector pods"
    prd: AGENT-DATA-003
spec:
  podSelector:
    matchLabels:
      app: erp-connector-service
      app.kubernetes.io/component: erp-connector
  policyTypes:
    - Ingress
    - Egress

---
# =============================================================================
# ERP Connector Service Ingress Policy
# =============================================================================

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: erp-connector-service-ingress
  namespace: greenlang
  labels:
    app.kubernetes.io/name: erp-connector-service
    app.kubernetes.io/component: network-policy
    app.kubernetes.io/part-of: greenlang
  annotations:
    description: "Allow ingress from Kong API Gateway, Prometheus, and greenlang namespace"
    prd: AGENT-DATA-003
spec:
  podSelector:
    matchLabels:
      app: erp-connector-service
      app.kubernetes.io/component: erp-connector
  policyTypes:
    - Ingress

  ingress:
    # Allow traffic from Kong API Gateway namespace
    - from:
        - namespaceSelector:
            matchLabels:
              app.kubernetes.io/name: kong-gateway
      ports:
        - protocol: TCP
          port: 8080

    # Allow traffic from monitoring namespace (Prometheus scraping)
    - from:
        - namespaceSelector:
            matchLabels:
              name: monitoring
      ports:
        - protocol: TCP
          port: 8080

    # Allow traffic from greenlang namespace (internal services)
    - from:
        - namespaceSelector:
            matchLabels:
              app.kubernetes.io/part-of: greenlang
      ports:
        - protocol: TCP
          port: 8080

    # Allow traffic within the greenlang namespace (inter-service)
    - from:
        - podSelector: {}
      ports:
        - protocol: TCP
          port: 8080

---
# =============================================================================
# ERP Connector Service Egress Policy
# =============================================================================

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: erp-connector-service-egress
  namespace: greenlang
  labels:
    app.kubernetes.io/name: erp-connector-service
    app.kubernetes.io/component: network-policy
    app.kubernetes.io/part-of: greenlang
  annotations:
    description: "Allow egress to PostgreSQL, Redis, ERP systems (HTTPS), OTEL collector, and DNS"
    prd: AGENT-DATA-003
spec:
  podSelector:
    matchLabels:
      app: erp-connector-service
      app.kubernetes.io/component: erp-connector
  policyTypes:
    - Egress

  egress:
    # Allow DNS resolution
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53

    # Allow egress to PostgreSQL (within cluster)
    - to:
        - namespaceSelector:
            matchLabels:
              name: database
      ports:
        - protocol: TCP
          port: 5432

    # Allow egress to PostgreSQL via AWS RDS (VPC CIDR)
    - to:
        - ipBlock:
            cidr: 10.0.0.0/8
      ports:
        - protocol: TCP
          port: 5432

    # Allow egress to Redis (within cluster)
    - to:
        - namespaceSelector:
            matchLabels:
              name: redis
      ports:
        - protocol: TCP
          port: 6379

    # Allow egress to Redis via AWS ElastiCache (VPC CIDR)
    - to:
        - ipBlock:
            cidr: 10.0.0.0/8
      ports:
        - protocol: TCP
          port: 6379

    # Allow egress to ERP systems (HTTPS for SAP, Oracle, Dynamics, NetSuite, etc.)
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - protocol: TCP
          port: 443

    # Allow egress to OTEL collector (monitoring namespace)
    - to:
        - namespaceSelector:
            matchLabels:
              name: monitoring
      ports:
        - protocol: TCP
          port: 4317
        - protocol: TCP
          port: 4318

    # Allow traffic within the greenlang namespace (other services)
    - to:
        - podSelector: {}
