---
# AGENT-DATA-015: GL-DATA-X-018 Cross-Source Reconciliation - Service
apiVersion: v1
kind: Service
metadata:
  name: cross-source-reconciliation-service
  namespace: greenlang
  labels:
    app: cross-source-reconciliation-service
    agent-id: GL-DATA-X-018
    agent: cross-source-reconciliation
    component: data-quality
spec:
  type: ClusterIP
  selector:
    matchLabels:
      app: cross-source-reconciliation-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
