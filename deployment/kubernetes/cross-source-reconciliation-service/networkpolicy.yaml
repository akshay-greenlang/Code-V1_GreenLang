---
# AGENT-DATA-015: GL-DATA-X-018 Cross-Source Reconciliation - NetworkPolicy
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cross-source-reconciliation-service
  namespace: greenlang
  labels:
    app: cross-source-reconciliation-service
    agent-id: GL-DATA-X-018
    agent: cross-source-reconciliation
    component: data-quality
spec:
  podSelector:
    matchLabels:
      app: cross-source-reconciliation-service
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Allow from Kong API Gateway
    - from:
        - podSelector:
            matchLabels:
              app: kong
      ports:
        - protocol: TCP
          port: 8000
    # Allow from Prometheus scraping
    - from:
        - podSelector:
            matchLabels:
              app: prometheus
      ports:
        - protocol: TCP
          port: 8000
    # Allow from other GreenLang data-quality agents
    - from:
        - podSelector:
            matchLabels:
              tier: data-quality
      ports:
        - protocol: TCP
          port: 8000
    - from:
        - podSelector:
            matchLabels:
              component: data-gateway
      ports:
        - protocol: TCP
          port: 8000
  egress:
    # PostgreSQL
    - to:
        - podSelector:
            matchLabels:
              app: postgres
      ports:
        - protocol: TCP
          port: 5432
    # Redis
    - to:
        - podSelector:
            matchLabels:
              app: redis
      ports:
        - protocol: TCP
          port: 6379
    # DNS
    - to: []
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # OTel Collector
    - to:
        - podSelector:
            matchLabels:
              app: otel-collector
      ports:
        - protocol: TCP
          port: 4317
    # Other GreenLang agents
    - to:
        - podSelector:
            matchLabels:
              tier: data-quality
      ports:
        - protocol: TCP
          port: 8000
