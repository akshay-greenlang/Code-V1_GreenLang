---
# AGENT-DATA-011: GL-DATA-X-014 Duplicate Detection Agent - HPA
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: duplicate-detector-service
  namespace: greenlang
  labels:
    app: duplicate-detector-service
    agent-id: GL-DATA-X-014
    component: duplicate-detection
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: duplicate-detector-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 120
      policies:
        - type: Pods
          value: 2
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Pods
          value: 1
          periodSeconds: 120
