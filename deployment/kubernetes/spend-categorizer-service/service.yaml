---
# AGENT-DATA-009: GL-DATA-SUP-002 Spend Data Categorizer - Service
apiVersion: v1
kind: Service
metadata:
  name: spend-categorizer-service
  namespace: greenlang
  labels:
    app: spend-categorizer-service
    agent-id: GL-DATA-SUP-002
    component: data-intake
  annotations:
    description: "Spend Data Categorizer ClusterIP service"
spec:
  type: ClusterIP
  selector:
    app: spend-categorizer-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  sessionAffinity: None
