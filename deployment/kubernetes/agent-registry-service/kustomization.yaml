# =============================================================================
# GreenLang Agent Registry & Service Catalog - Kustomization
# =============================================================================
# Use: kubectl apply -k deployment/kubernetes/agent-registry-service/
# PRD: AGENT-FOUND-007
# =============================================================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: agent-registry-service
  annotations:
    description: "GreenLang Agent Registry & Service Catalog Deployment"
    prd: AGENT-FOUND-007

# Namespace for all resources
namespace: greenlang

# Common labels applied to all resources
commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: greenlang
  app.kubernetes.io/version: "1.0.0"

# Common annotations
commonAnnotations:
  prd: AGENT-FOUND-007

# Resources to include (order matters: config first, then services, deployments)
resources:
  - configmap.yaml
  - networkpolicy.yaml
  - service.yaml
  - deployment.yaml
  - hpa.yaml
  - servicemonitor.yaml

# Image transformations (update tags easily)
images:
  - name: greenlang/agent-registry-service
    newTag: latest

# Config transformations
configMapGenerator:
  - name: agent-registry-service-deployment-info
    literals:
      - DEPLOYMENT_VERSION=1.0.0
      - DEPLOYMENT_DATE=2026-02-08
