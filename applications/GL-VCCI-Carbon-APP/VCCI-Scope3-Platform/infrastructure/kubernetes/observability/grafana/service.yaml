---
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: vcci-observability
  labels:
    app: grafana
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  selector:
    app: grafana
