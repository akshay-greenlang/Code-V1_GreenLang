/**
 * TargetsPage - Reduction targets and SBTi alignment
 * Placeholder component; full implementation in a subsequent build phase.
 */

import React from 'react';
import { Typography, Box } from '@mui/material';

const TargetsPage: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Reduction Targets
      </Typography>
      <Typography variant="body1" color="text.secondary">
        Set and track emission reduction targets with SBTi alignment checking.
      </Typography>
    </Box>
  );
};

export default TargetsPage;
