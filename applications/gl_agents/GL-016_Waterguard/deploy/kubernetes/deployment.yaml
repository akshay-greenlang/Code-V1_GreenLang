# GL-016 WaterGuard - Kubernetes Deployment
# Generated by GreenLang Enhancement Script
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gl-016-waterguard
  labels:
    app: gl-016
    agent: waterguard
    greenlang.io/agent-id: "GL-016"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gl-016
  template:
    metadata:
      labels:
        app: gl-016
        agent: waterguard
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: gl-016-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: agent
          image: greenlang/gl-016:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: grpc
              containerPort: 50051
              protocol: TCP
          env:
            - name: AGENT_ID
              value: "GL-016"
            - name: AGENT_NAME
              value: "WaterGuard"
            - name: LOG_LEVEL
              value: "INFO"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector:4317"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          volumeMounts:
            - name: config
              mountPath: /app/config
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: gl-016-config
