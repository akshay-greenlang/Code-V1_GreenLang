# GL-008 Trapcatcher Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gl-008-trapcatcher
  namespace: greenlang
  labels:
    app: gl-008-trapcatcher
    agent-id: GL-008
    codename: TRAPCATCHER
    version: v1.0.0
    tier: production
    domain: steam-trap-diagnostics
  annotations:
    kubernetes.io/description: "GL-008 Trapcatcher - Steam Trap Diagnostics Agent"
    prometheus.io/scrape: "true"
    prometheus.io/port: "9090"
    prometheus.io/path: "/metrics"
spec:
  replicas: 2
  revisionHistoryLimit: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: gl-008-trapcatcher
  template:
    metadata:
      labels:
        app: gl-008-trapcatcher
        agent-id: GL-008
        codename: TRAPCATCHER
        version: v1.0.0
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: gl-008-trapcatcher-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: gl-008-trapcatcher
                topologyKey: kubernetes.io/hostname
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: gl-008-trapcatcher
      containers:
        - name: gl-008-trapcatcher
          image: gcr.io/greenlang/gl-008-trapcatcher:v1.0.0
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          env:
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: gl-008-trapcatcher-config
                  key: ENVIRONMENT
            - name: GL008_LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: gl-008-trapcatcher-config
                  key: LOG_LEVEL
            - name: GL008_API_PORT
              value: "8080"
            - name: GL008_METRICS_PORT
              value: "9090"
            - name: GL008_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: gl-008-trapcatcher-secrets
                  key: database-url
            - name: GL008_REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: gl-008-trapcatcher-secrets
                  key: redis-url
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "4Gi"
              cpu: "2000m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 30
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: config-volume
              mountPath: /app/config
              readOnly: true
            - name: tmp-volume
              mountPath: /tmp
            - name: logs-volume
              mountPath: /var/log/greenlang
            - name: trap-data-volume
              mountPath: /var/data/steam_trap_diagnostics
      volumes:
        - name: config-volume
          configMap:
            name: gl-008-trapcatcher-config
        - name: tmp-volume
          emptyDir: {}
        - name: logs-volume
          emptyDir: {}
        - name: trap-data-volume
          emptyDir: {}
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      restartPolicy: Always
