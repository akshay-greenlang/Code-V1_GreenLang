apiVersion: v1
kind: Service
metadata:
  name: gl-comply-hub
  labels:
    app.kubernetes.io/name: gl-comply-hub
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: gl-comply-hub
