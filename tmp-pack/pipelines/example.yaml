name: example-pipeline
version: 1.0.0
kind: pipeline
description: Example pipeline for tmp-pack
stages:
  - name: hello
    agent: basic
    inputs:
      message: "Hello from tmp-pack!"
    outputs:
      result: string