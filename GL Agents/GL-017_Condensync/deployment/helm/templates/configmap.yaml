{{/*
GL-017_Condensync Helm Chart - ConfigMap Template
Condenser Optimization Agent
*/}}
{{- if .Values.configMap.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gl-017-condensync.configMapName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gl-017-condensync.labels" . | nindent 4 }}
data:
  # Agent Configuration
  AGENT_ID: {{ .Values.agent.id | quote }}
  AGENT_NAME: {{ .Values.agent.name | quote }}
  AGENT_FULL_NAME: {{ .Values.agent.fullName | quote }}
  AGENT_GROUP: {{ .Values.agent.group | quote }}

  # ConfigMap data from values
  {{- range $key, $value := .Values.configMap.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
