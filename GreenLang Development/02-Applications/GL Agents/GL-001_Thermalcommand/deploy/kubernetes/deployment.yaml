# GL-001 ThermalCommand Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gl-001-thermalcommand
  namespace: greenlang
  labels:
    app: gl-001-thermalcommand
    agent-id: GL-001
    codename: THERMALCOMMAND
    version: v1.0.0
    tier: production
    domain: thermal-optimization
  annotations:
    kubernetes.io/description: "GL-001 ThermalCommand - Multi-Equipment Thermal Asset Optimization Agent"
    prometheus.io/scrape: "true"
    prometheus.io/port: "9090"
    prometheus.io/path: "/metrics"
spec:
  replicas: 2
  revisionHistoryLimit: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: gl-001-thermalcommand
  template:
    metadata:
      labels:
        app: gl-001-thermalcommand
        agent-id: GL-001
        codename: THERMALCOMMAND
        version: v1.0.0
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: gl-001-thermalcommand-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: gl-001-thermalcommand
                topologyKey: kubernetes.io/hostname
            - weight: 50
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: gl-001-thermalcommand
                topologyKey: topology.kubernetes.io/zone
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: gl-001-thermalcommand
      containers:
        - name: gl-001-thermalcommand
          image: gcr.io/greenlang/gl-001-thermalcommand:v1.0.0
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          env:
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: gl-001-thermalcommand-config
                  key: ENVIRONMENT
            - name: GL001_LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: gl-001-thermalcommand-config
                  key: LOG_LEVEL
            - name: GL001_API_PORT
              value: "8080"
            - name: GL001_METRICS_PORT
              value: "9090"
            - name: GL001_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: gl-001-thermalcommand-secrets
                  key: database-url
            - name: GL001_REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: gl-001-thermalcommand-secrets
                  key: redis-url
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "4Gi"
              cpu: "2000m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
              scheme: HTTP
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
            successThreshold: 1
          startupProbe:
            httpGet:
              path: /health
              port: 8080
              scheme: HTTP
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: config-volume
              mountPath: /app/config
              readOnly: true
            - name: tmp-volume
              mountPath: /tmp
            - name: logs-volume
              mountPath: /var/log/greenlang
            - name: data-volume
              mountPath: /var/data/greenlang
            - name: optimization-volume
              mountPath: /var/data/thermal_optimization
      volumes:
        - name: config-volume
          configMap:
            name: gl-001-thermalcommand-config
        - name: tmp-volume
          emptyDir: {}
        - name: logs-volume
          emptyDir: {}
        - name: data-volume
          emptyDir: {}
        - name: optimization-volume
          emptyDir: {}
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      restartPolicy: Always
