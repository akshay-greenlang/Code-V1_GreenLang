# {{ cookiecutter.agent_name }}

{{ cookiecutter.description }}

## Overview

- **Agent ID**: `{{ cookiecutter.agent_id }}`
- **Version**: `{{ cookiecutter.version }}`
- **Type**: {{ cookiecutter.agent_type }}
- **Domain**: {{ cookiecutter.domain }}
- **Status**: Active

## Features

{% for capability in cookiecutter.capabilities %}
- {{ capability }}
{% endfor %}

## Installation

```bash
# Install dependencies
pip install -r requirements.txt

# Or using Docker
docker build -t {{ cookiecutter.agent_id }}:{{ cookiecutter.version }} .
```

## Quick Start

```python
from agent import {{ cookiecutter.agent_class_name }}, {{ cookiecutter.agent_class_name }}Input

# Initialize agent
agent = {{ cookiecutter.agent_class_name }}()

# Create input
input_data = {{ cookiecutter.agent_class_name }}Input(
    # Add your input parameters here
)

# Process
result = agent.process(input_data)

# Access results
print(f"Result: {result.result}")
print(f"Provenance Hash: {result.provenance_hash}")
print(f"Status: {result.validation_status}")
```

## Input Schema

| Field | Type | Required | Description |
|-------|------|----------|-------------|
{% for field in cookiecutter.input_fields %}
| {{ field.name }} | {{ field.type }} | {{ field.required }} | {{ field.description }} |
{% endfor %}

## Output Schema

| Field | Type | Description |
|-------|------|-------------|
| result | Any | Primary processing result |
| provenance_hash | str | SHA-256 hash for audit trail |
| processing_time_ms | float | Processing duration in milliseconds |
| validation_status | str | PASS or FAIL |
{% for field in cookiecutter.output_fields %}
| {{ field.name }} | {{ field.type }} | {{ field.description }} |
{% endfor %}

## Regulatory Compliance

This agent complies with:

{% for framework in cookiecutter.frameworks %}
- **{{ framework }}**
{% endfor %}

{% for standard in cookiecutter.standards %}
- {{ standard }}
{% endfor %}

## Certification

This agent is designed to meet GreenLang's 12-dimension certification framework:

| Dimension | Description | Status |
|-----------|-------------|--------|
| D01 | Determinism | Pending |
| D02 | Provenance Tracking | Pending |
| D03 | Zero-Hallucination | Pending |
| D04 | Accuracy | Pending |
| D05 | Source Verification | Pending |
| D06 | Unit Consistency | Pending |
| D07 | Regulatory Compliance | Pending |
| D08 | Security | Pending |
| D09 | Performance | Pending |
| D10 | Documentation | Pending |
| D11 | Test Coverage | Pending |
| D12 | Production Readiness | Pending |

Run certification:
```bash
gl agent certify {{ cookiecutter.agent_id }}
```

## Testing

```bash
# Run all tests
pytest

# Run with coverage
pytest --cov=agent --cov-report=html

# Run specific test types
pytest -m unit           # Unit tests only
pytest -m integration    # Integration tests only
pytest -m golden         # Golden tests only
pytest -m compliance     # Compliance tests only
```

## Architecture

```
{{ cookiecutter.agent_id }}/
├── agent.py           # Main agent implementation
├── agent.yaml         # Agent configuration
├── requirements.txt   # Python dependencies
├── Dockerfile         # Container definition
├── README.md          # This file
└── tests/
    ├── conftest.py      # Test configuration
    ├── test_unit.py     # Unit tests
    ├── test_integration.py  # Integration tests
    ├── test_golden.py   # Golden tests
    └── test_compliance.py   # Compliance tests
```

## Zero-Hallucination Principle

This agent follows GreenLang's zero-hallucination principle:

1. **No LLM in Calculation Path**: All numeric calculations use deterministic formulas
2. **Validated Data Sources**: Emission factors come from verified databases
3. **Provenance Tracking**: Every output includes SHA-256 hash for audit trail
4. **Reproducibility**: Same inputs always produce identical outputs

## API Reference

### `{{ cookiecutter.agent_class_name }}`

Main agent class.

#### Methods

- `__init__(config: Optional[Dict] = None)`: Initialize agent
- `process(input_data: {{ cookiecutter.agent_class_name }}Input) -> {{ cookiecutter.agent_class_name }}Output`: Process input data

## Deployment

### Docker

```bash
# Build image
docker build -t {{ cookiecutter.agent_id }}:{{ cookiecutter.version }} .

# Run container
docker run -p 8080:8080 {{ cookiecutter.agent_id }}:{{ cookiecutter.version }}
```

### Kubernetes

```bash
# Deploy using GreenLang CLI
gl agent deploy {{ cookiecutter.agent_id }} --env production --tag {{ cookiecutter.version }}
```

## Support

- **Documentation**: [GreenLang Docs](https://docs.greenlang.io)
- **Issues**: [GitHub Issues](https://github.com/greenlang/agents/issues)
- **Email**: support@greenlang.io

## License

{{ cookiecutter.license }}

---

*Auto-generated by GreenLang Agent Factory*
