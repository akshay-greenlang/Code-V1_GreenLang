# PostgreSQL + TimescaleDB Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - statefulset.yaml
  - configmap.yaml

commonLabels:
  app.kubernetes.io/component: postgresql
