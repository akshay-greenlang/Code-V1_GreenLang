# Invalid Payload - Enum Violation (numeric)
# Error Code: GLSCHEMA-E202 (ENUM_VIOLATION)
# Schema: basic/enum_constraints.yaml

_test_metadata:
  schema: "basic/enum_constraints.yaml"
  description: "Enum violation - priority out of range"
  expected_valid: false
  expected_errors:
    - code: "GLSCHEMA-E202"
      path: "/priority"
      severity: "error"

status: "approved"
priority: 10  # Not in allowed enum [1,2,3,4,5]
