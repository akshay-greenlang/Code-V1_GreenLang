# Invalid Payload - Length Violation (too short)
# Error Code: GLSCHEMA-E203 (LENGTH_VIOLATION)
# Schema: basic/string_constraints.yaml

_test_metadata:
  schema: "basic/string_constraints.yaml"
  description: "Length violation - username too short"
  expected_valid: false
  expected_errors:
    - code: "GLSCHEMA-E203"
      path: "/username"
      severity: "error"

username: "ab"  # minLength is 3
email: "user@example.com"
